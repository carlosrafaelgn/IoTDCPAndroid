<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BgTextLarge" parent="@style/Base.TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/colorBgText</item>
    </style>

    <style name="BgTextMedium" parent="@style/Base.TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/colorBgTextMedium</item>
    </style>

    <style name="BgTextSecondary" parent="@style/Base.TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/colorBgTextSecondary</item>
    </style>

    <style name="BgDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorBgText</item>
        <item name="android:textColorSecondary">@color/colorBgTextSecondary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
</resources>
